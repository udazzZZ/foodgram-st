apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ $.Values.global.namespace }}
  name: {{ $.Values.global.config.redis }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}