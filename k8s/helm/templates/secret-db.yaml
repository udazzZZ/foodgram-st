apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.postgres.secret.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  POSTGRES_USER: "{{ .Values.postgres.secret.username }}"
  POSTGRES_PASSWORD: "{{ .Values.postgres.secret.password }}"
  POSTGRES_DB: "{{ .Values.postgres.secret.database }}"
